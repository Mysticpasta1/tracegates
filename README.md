This mod adds logic gates from Virtual Circuit Board.

The logic gates are compiled into a graph that lets them run at higher speeds, and through and inside unloaded chunks.

`/tracegates tick` lets you control the speed of the simulation.

clicking and shift-clicking lets you cycle through different traces, for convenience.